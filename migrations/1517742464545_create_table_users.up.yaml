- args:
    sql: CREATE TABLE "users"("id" integer NOT NULL, "hasura_id" integer NOT NULL,
      "username" text NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
